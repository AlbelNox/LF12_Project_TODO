// import type { TodoList } from "@/types/TodoList";
// import type { createApiService } from "./apiService";
// import {Routes, ServerAddress} from "../config/serverConfig"

// export function createTodoListService(api: ReturnType<typeof createApiService>) {
//   return {
//     getTodoLists: () => api.get<TodoList[]>(`${ServerAddress}${Routes.GET_LISTS}`),

//     /* getUserById: (id: number) => api.get<User>(`/users/${id}`),

//     createUser: (dto: CreateUserDto) => api.post<User>("/users", dto),

//     deleteUser: (id: number) => api.delete<void>(`/users/${id}`), */
//   };
// }
